{
  "name": "@cardspark/tokens",
  "private": true,
  "version": "0.1.0",
  "description": "Design tokens and CSS variables for Cardspark UI.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "keywords": [
    "cards",
    "tcg",
    "design-tokens",
    "css"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.build.json && cp src/tokens.css dist/tokens.css",
    "prepack": "npm run build",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./tokens.css": "./dist/tokens.css"
  },
  "sideEffects": [
    "**/*.css"
  ]
}
